import { Request, Response } from "express";
import { ItemModel } from "../models/Item";


export const addItem = async (req: Request, res: Response) => {

    // try{

    //     const item_id = req.body.id

    //     const is_item = await ItemModel.findOne({ id: item_id });

    //     if(is_item == null){
    //         if(req.body != null){
    //             new ItemModel
    //     }else{

    //     }

    // }catch{

    // }

}